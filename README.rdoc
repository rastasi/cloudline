in the project folder:

* gem install bundler (if not installed)
* bundle install
* edit config/database.yml (if required)
* rake db:create (if you use root user)
* rake db:migrate
* rake db:seed
* rails s -b 0.0.0.0

default user/pass: admin@example.com / password
